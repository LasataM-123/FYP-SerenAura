import Constants from "expo-constants";

export const API_URL = Constants.expoConfig?.extra?.API_URL as string;

export const GOOGLE_CLIENT_ID = Constants.expoConfig?.extra?.GOOGLE_CLIENT_ID as string;

export const WEB_CLIENT_ID = Constants.expoConfig?.extra?.WEB_CLIENT_ID as string;